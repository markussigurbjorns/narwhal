NARWHAL TO DO:

  1. Tune the buffer sizes based on actual bitrate and latency targets.
  2. Add counters or metrics for dropped RTP so overload is visible in production.
  3. Decide whether slow subscribers should keep dropping packets or be disconnected after sustained overflow.

1. Relay keyframe feedback only: detect subscriber-side PLI/FIR and trigger a publisher-side keyframe request.
  2. Add observability first: inspect what feedback webrtcbin/stats expose so we know what to translate.
  3. Go lower-level: integrate with rtpbin/transceiver hooks instead of pad-level RTP copying.


IF a new publisher starts, then we dont know where to send the kayframes

• 1. Run a fresh publish+subscribe and capture server logs around subscribe time.
  2. Check if join_to_first_rtp_ms is low but join_to_first_video_keyframe_ms is high. If yes, keyframe cadence/request path is the bottleneck.
  3. Check for dropping RTP ... queue is full. If present, raise queue sizes or drop slow subscribers earlier.
  4. If keyframe delay is the issue, send periodic FIR/PLI for first 2-3 seconds after subscribe (until first video keyframe seen).
  5. Add one integration test scenario: subscribe while publisher is already running, assert first media arrival under a target SLA (for example < 1500 ms).

MEETING MODE 
Execution plan

  1. Introduce room mode + meeting state structs (no behavior change yet).
  2. Add WS + JSON-RPC plumbing.
  3. Implement join/leave + ICE + sdp_offer.
  4. Implement publish_tracks + subscribe/unsubscribe graph.
  5. Wire renegotiation revisioning + serialization lock.
  6. Add tests for race cases (simultaneous subscribe/unsubscribe, stale revision).
